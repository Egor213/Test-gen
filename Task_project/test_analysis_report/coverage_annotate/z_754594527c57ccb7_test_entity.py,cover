! from entity.entity import Task
! import pytest
  
  
! class TestTask:
!     @pytest.fixture
!     def task(self):
!         return Task("Test task", 2)
  
!     def test_mark_completed_sets_completed_to_true(self, task):
!         """Тест метода mark_completed устанавливает флаг completed в True"""
!         task.mark_completed()
!         assert task.completed is True
  
!     def test_mark_completed_changes_status_from_false_to_true(self, task):
!         """Тест метода mark_completed изменяет статус с False на True"""
!         assert task.completed is False
!         task.mark_completed()
!         assert task.completed is True
  
!     def test_mark_completed_is_idempotent(self, task):
!         """Тест метода mark_completed идемпотентен (не меняет состояние при повторном вызове)"""
!         task.mark_completed()
!         initial_completed = task.completed
!         task.mark_completed()
!         assert task.completed is initial_completed
