{% if syntax_error %}
Исправь синтаксические ошибки в pytest тестах.

Ошибочный код:
```python
{{ code_to_fix }}
```

Ошибки компиляции:
```
{{ error_output }}
```

Инструкции по исправлению:
1. Проверь отступы (должны быть 4 пробела)
2. Проверь импорты (правильные имена модулей)
3. Исправь синтаксис декораторов
4. Убери лишние скобки/кавычки
5. Проверь имена переменных и атрибутов

Особенности pytest:
- Декораторы должны быть перед функциями без пустых строк
- `@pytest.fixture` должен возвращать значение или иметь `yield`
- `@pytest.mark.parametrize` должен иметь правильное количество параметров

{% elif test_failure %}
Исправь логические ошибки в pytest тестах на основе результатов выполнения.

Код теста:
```python
{{ code_to_fix }}
```

Результат выполнения:
```
{{ test_output }}
```

Трассировка:
```
{{ traceback }}
```

Инструкции:
1. Проанализируй причину падения теста
2. Исправь логику теста или ассерты
3. Убедись, что тест проверяет правильное поведение
4. Исправь моки и фикстуры если нужно
5. Проверь, что тест изолирован и воспроизводим

{% elif import_error %}
Исправь ошибки импорта в pytest тестах.

Код с ошибками импорта:
```python
{{ code_to_fix }}
```

Ошибки импорта:
```
{{ import_errors }}
```

Инструкции:
1. Проверь пути импорта
2. Убедись, что модуль существует
3. Исправь циклические импорты
4. Добавь недостающие импорты
5. Исправь относительные импорты

{% endif %}

@output{исправленный код}: верни исправленный pytest тест